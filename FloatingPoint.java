package operators;
/**
 <p>
 * Float data type (also known as floating point data type, or real number) is 
 * a number data type that has a decimal part at the end of the number,or has floating point (floating point is an English term to refer to the "dot " sign which indicates a decimal number) .
 <p>
 * author   : Egi Satria 
 <p>
 * since    : 2022-02-11
 <p>
 */

public class FloatingPoint {
    public static void main(String[] args) {
     double x = 92.98;
        int nx = (int) Math.round(x);
        System.out.println("hasil = " + nx);
    }
    
}